{
  "private": true,
  "scripts": {
    "build": "nuxt build",
    "dev": "nuxt dev",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare"
  },
  "devDependencies": {
    "mongoose": "^7.0.0",
    "mongoose-bcrypt": "^1.10.1",
    "nuxt": "^3.2.3"
  },
  "dependencies": {
    "@picocss/pico": "^1.5.7"
  }
}
